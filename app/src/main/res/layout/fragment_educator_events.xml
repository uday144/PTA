<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:schoolo="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.ptapp.widget.CollectionView
        android:id="@+id/events_collection_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingLeft="@dimen/explore_grid_padding"
        android:paddingRight="@dimen/explore_grid_padding"
        android:paddingBottom="@dimen/explore_grid_padding"
        schoolo:internalPadding="@dimen/explore_grid_padding" />

    <!-- Empty view -->
    <TextView
        android:id="@android:id/empty"
        android:visibility="gone"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:textSize="@dimen/text_size_medium"
        android:textColor="@color/body_text_2"
        android:text="@string/no_matching_videos"
        android:paddingTop="?android:actionBarSize" />
</FrameLayout>
